// Description:
// Write a function which converts the input string to uppercase.

// My Solution:
export const makeUpperCase = (str: string): string => str.toUpperCase();

// Best Practice Solutions:
/*
1. Solution by ejini战神 (Best Practices: 61, Clever: 1)

export function makeUpperCase(str: string): string {
  return str.toUpperCase();
}


2. Solution by N4m1k4ze (Best Practices: 12, Clever: 8)

export const makeUpperCase = (str: string): string => str.toUpperCase();
*/
